api: yes
apiAddress: :9997

authInternalUsers:
  - user: any
    pass:
    ips: []
    permissions:
      - action: publish
      - action: read
      - action: playback
      - action: api
      - action: metrics

paths:
  test:
    source: publisher
    record: no
    recordPath: /storage/recording/%path_%Y-%m-%d_%H-%M-%S-%f
    recordFormat: fmp4
    recordSegmentDuration: 1m
    runOnRecordSegmentComplete: /hooks/notify-segment-complete.sh /storage/completed /storage/error
